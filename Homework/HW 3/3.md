
<div align="center">

Homework 3: Financial Data Analysis

FMA4200@CUHK (SZ)

Due on March 28, 2026 (11:59pm)

</div>

Submit an electronic version of your answer to Blackboard (late submission will not be accepted).

1. Answer the below question as you are a test taker according to all the files I've uploaded:
Consider a MA(2) process:
$$
X_t = \mu + \varepsilon_t + a_1\varepsilon_{t-1} + a_2\varepsilon_{t-2},
$$
where $\varepsilon_t$ is a white noise process with mean $0$ and variance $\sigma^2$.

Do you think the bitcoin returns can be modeled by a MA($q$) model? Verify your answer with the data provided in the attached file and write all your answer in .

给你上传的图中是我有的python环境。我清楚明白地告诉你：在目前选定的环境（`base (Python 3.12.7) `）中是有 pandas 的，而且我想用 pandas 来实现所有要求。
以及你只用先在 markdown 格子中写一些公式以及背景信息。具体的结论我在自己跑了一遍、运算了一遍之后会再让你根据我电脑上的数据再写的！
你在 [hw3.ipynb](hw3.ipynb) 生成的所有内容都要使用英语！

(a) Is the model stationary?
(b) Write down the invertibility condition of the model. Be as explicit as possible.
(c) Find the mean, variance, autocovariance function and autocorrelation function of $X_t$.
(d) Do you think the bitcoin returns can be modeled by a MA($q$) model? Verify your answer with the data provided in the attached file.

> [!info]+ 1
> 
> 考虑一个 MA(2) 过程：
> $$
> X_t = \mu + \varepsilon_t + a_1\varepsilon_{t-1} + a_2\varepsilon_{t-2},
> $$
> 其中 $\varepsilon_t$ 是一个均值为 $0$、方差为 $\sigma^2$ 的白噪声过程。
> 
> (a) 该模型是平稳的吗？
> (b) 写出该模型的可逆条件。尽可能写得明确。
> (c) 求 $X_t$ 的均值、方差、自协方差函数和自相关函数。
> (d) 你认为比特币收益率可以用 MA($q$) 模型来建模吗？请利用所附文件中提供的数据验证你的答案。

以上是第一问。下面是第二问：

2. Answer the below question as you are a test taker according to all the files I've uploaded:
Consider an AR(2) process:
$$
X_t = c + b_1X_{t-1} + b_2X_{t-2} + \varepsilon_t,
$$
where $\varepsilon_t$ is a white noise process with mean $0$ and variance $\sigma^2$.

(a) Is the model invertible?
(b) Write down the stationary condition of the AR(2) process. Be as explicit as possible.
(c) Find the mean, variance, autocovariance function and autocorrelation function of $X_t$.
(d) Do you think the bitcoin returns can be modeled by an AR($p$) model? Verify your answer with the data provided in the attached file.

> [!info]+ 2
> 
> 考虑一个 AR(2) 过程：
> $$
> X_t = c + b_1X_{t-1} + b_2X_{t-2} + \varepsilon_t,
> $$
> 其中 $\varepsilon_t$ 是一个均值为 $0$、方差为 $\sigma^2$ 的白噪声过程。
> 
> (a) 该模型是可逆的吗？
> (b) 写出 AR(2) 过程的平稳条件。尽可能写得明确。
> (c) 求 $X_t$ 的均值、方差、自协方差函数和自相关函数。
> (d) 你认为比特币收益率可以用 AR($p$) 模型来建模吗？请利用所附文件中提供的数据验证你的答案。
